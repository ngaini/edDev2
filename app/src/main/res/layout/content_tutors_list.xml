<?xml version="1.0" encoding="utf-8"?>

<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:layout_marginTop="?attr/actionBarSize"
    tools:context="edu.scu.mapproject.TutorsListActivity"
    android:id="@+id/tutorDrawerLayout">



    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/tutorMainContent">
        <ListView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/tutorActivity_studentList_listView"
            android:layout_marginTop="10dp"
            android:background="#f1f0ee">

        </ListView>
    </FrameLayout>

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/tutorDrawerList"
        android:layout_gravity="left"
        android:divider="@null"
        android:background="#FFBDBCB9"
        android:paddingLeft="5dp"
        android:paddingTop="20dp"
        android:gravity="center"
        android:entries="@array/navOptions">

    </ListView>

</android.support.v4.widget.DrawerLayout>